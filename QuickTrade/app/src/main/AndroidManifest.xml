<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="josevi.android.com.quicktrade">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        -->
        <activity android:name=".RegistrarActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".InsertarActivity" />
        <activity android:name=".ModificarActivity" />
        <activity android:name=".MostrarActivity" />
        <!-- <activity android:name=".RegistrarActivity"></activity> -->
        <activity android:name=".GestorProductosActivity" />
        <activity android:name=".MostrarEnVentaActivity" />
        <activity android:name=".MostrarPorCategoriaActivity" />
        <activity android:name=".ModificarProductoActivity" />
        <activity android:name=".BorrarProductoActivity"></activity>
    </application>

</manifest>